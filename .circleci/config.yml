version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.3
    filters:
      branches:
        only: master
    steps:
      - checkout
      # - restore_cache:
      #   keys:
      #     - node_modules
      - run: ll -ah ~/usr/local/lib/node_modules
      - run: vuepress -V
      # - run: sudo npm i -g vuepress
      # - save_cache:
      #     paths:
      #       - ~/usr/local/lib/node_modules
      #     key: node_modules
      # - run: sh deploy.circleci.sh
