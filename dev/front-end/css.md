# css

``` css
/* 渐变 默认从上到下，0deg从下向上 */
background-image: linear-gradient(90deg, #D1494F 0%, #DE8063 100%);
```

``` css
/* 超出隐藏 */
.ellipsis{
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```